/**
 * Frontend configuration.
 *
 * When served by Flask from the same origin, keep API_BASE_URL empty.
 */
export const API_BASE_URL = "";

export const SUPPORTED_CURRENCIES = ["DKK", "EUR", "USD"];
