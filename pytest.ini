[pytest]
pythonpath = .
testpaths = tests
markers =
	integration: full-stack tests (Flask -> services -> repos -> SQLite, FX stubbed)
	e2e: end-to-end tests (browser UI via Playwright; may require running server)
